rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // AERO browsers never access Firestore directly. All reads and writes go
    // through the authenticated API server, whose Admin SDK bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
